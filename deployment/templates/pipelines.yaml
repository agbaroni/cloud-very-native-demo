apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: launch-builds
  namespace: cvnd
spec:
  tasks:
    - name: launch-build-common
      params:
        - name: build-name
          value: common
      taskRef:
        name: launch-build
